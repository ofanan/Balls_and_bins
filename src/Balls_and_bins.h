#ifndef BALLS_AND_BINS_H
#define BALLS_AND_BINS_H

# include <stdio.h>

typedef int16_t Bin_t; //May change bin type to reduce sim time
const int MAX_BIN_VAL = std::numeric_limits<Bin_t>::max(); // max representatable value for Bin_t.

#endif
