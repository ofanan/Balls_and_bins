# include "settings.h"

//class settings;
//bool settings::file_exists (const std::string& name) // returns true iff the given fileName already exists
//{
//	struct stat buffer;
//	return (name.c_str(), &buffer) == 0;
//}
//

void func_in_settings (ofstream log_file)
{
	return;
}

//void settings::printToLog (vector <ChainId_t> vec)
//{
//	logFile << ",";
//	for (const auto chainId : vec) {
//		logFile << "c" << chainId << ",";
//	}
//}
